# План: самовосстановление и самоулучшение Евы

Дата: 2026-08-12
Ревизия: v2 (после самостоятельного ревью)
Статус: черновик, ожидает внешнего ревью
Область: `evohime-core` (agent loop), `evohime-tool-runtime` (registry, tools), `evohime-model-gateway` (retry/routing), `evohime-supervisor` (lifecycle), `evohime-local-storage` (memory)

## 1. Проблема

Агентный цикл Евы (`ToolAgent::run_once_with_cancellation`, [lib.rs:1907](../../crates/evohime-core/src/lib.rs:1907)) сейчас:

- определяет успех инструмента поиском подстрок в человекочитаемом тексте вывода;
- отдаёт одну и ту же recovery-подсказку на любой класс ошибки;
- не накапливает никакого состояния между задачами — каждая задача стартует с нуля;
- не измеряет собственную эффективность, поэтому любое «улучшение» недоказуемо.

Supervisor восстанавливает Core после падения, но бюджет рестартов монотонный и не сбрасывается. Gateway ретраит только запрос до начала SSE.

Цель плана — заменить текстовые эвристики типизированным результатом, довести восстановление до осознанного (классификация → адресная подсказка → эскалация), и замкнуть петлю обучения через уже существующее, но неподключённое хранилище памяти.

## 2. Границы

Не входит в объём:

- vector/RAG-поиск по памяти (остаётся lexical, как задано в `memory_domain`);
- автоматическое изменение Евой собственного исходного кода или промптов без approval;
- перенос состояния из Core в UI;
- смена контракта `desktop-ipc-v1` major-версии.

Инварианты сохраняются: sandbox, таймауты, отмена, approval для опасных инструментов, отсутствие секретов в логах и памяти.

## 3. Общие требования к внедрению

**Kill-switch.** Каждая фаза, меняющая поведение живого прогона (1, 2, 3, 5.3), закрывается env-флагом с безопасным значением по умолчанию на время обкатки. Флаги снимаются после подтверждения метриками, а не «на глаз».

**Shadow-режим перед строгим.** Фазы 1 и 5.3 сначала работают параллельно старой эвристике: новая оценка только логируется в trace, решения принимает старая. Строгий режим включается после сверки расхождений на реальных прогонах.

**Размещение кода.** `lib.rs` уже 5097 строк. Новая логика идёт в отдельные модули `recovery.rs`, `run_metrics.rs`, `task_memory.rs`; в `lib.rs` остаются только точки вызова.

## 4. Фазы

Фазы упорядочены по зависимостям. Фаза 1 — предпосылка для 2, 3 и 5; фаза 4 независима.

---

### Фаза 1. Типизированный результат инструмента

**Задача 1.1. Классификация ошибок в Core, без изменения `ToolResult`.**

`execute_with_cancellation` возвращает `Result<ToolResult, ToolError>` ([registry.rs:11](../../crates/tool-runtime/src/registry.rs:11)). Для провалов сигнал уже типизирован — это вариант `ToolError`. Поэтому `ToolResult` не трогаем, а в новом модуле `recovery.rs` вводим:

```rust
pub enum ToolFailureKind {
    NotFound,        // ресурс не существует
    InvalidInput,    // ToolError::InvalidInput, битый JSON аргументов
    Denied,          // PermissionDenied, отклонённый approval
    Timeout,         // ToolError::TimedOut
    NonZeroExit,     // shell.execute с exit_code != 0
    Execution,       // ToolError::Execution и прочее
}

pub struct ToolOutcome {
    pub ok: bool,
    pub kind: Option<ToolFailureKind>,
    pub output: String,      // текст для модели
}

impl ToolOutcome {
    fn from_result(tool: &str, result: &Result<ToolResult, ToolError>) -> Self { ... }
}
```

Порядок определения `ok`: (1) вариант `ToolError`; (2) поле `structured` для инструментов из задачи 1.2; (3) `ok = true`. Текстовая эвристика `tool_output_failed` удаляется целиком, а не остаётся fallback-ом: сохранённый fallback вернёт ровно те ложные срабатывания, ради которых затевается фаза.

**Задача 1.2. Инструменты, возвращающие `Ok` при семантическом провале.**

Требовать поле `ok` во всех `structured` не нужно — 15 из 17 мест возвращают `Ok` только при фактическом успехе. Маскируют провал ровно два:

- `shell.execute` ([shell.rs:158](../../crates/tool-runtime/src/tools/shell.rs:158)) — `Ok` с любым `exit_code`; читать `structured["exit_code"]` и `structured["timed_out"]`;
- `git.commit` ([git.rs:80](../../crates/tool-runtime/src/tools/git.rs:80)) — `Ok` со `status: "nothing_to_commit"`; это не ошибка инструмента, но и не выполненный коммит, поэтому `commit_done` выставлять нельзя.

`http.fetch` уже возвращает `Err` на не-2xx ([http.rs:78](../../crates/tool-runtime/src/tools/http.rs:78)) — менять не нужно.

Тест: итерация по реестру, проверяющая, что ни один другой инструмент не возвращает `Ok` с признаком ошибки в `structured`.

**Задача 1.3. Заменить `tool_output_failed` в agent loop.**

[lib.rs:2239](../../crates/evohime-core/src/lib.rs:2239) — вместо `let failed = tool_output_failed(&output)` использовать `outcome.ok`. Ветка approval ([lib.rs:2205](../../crates/evohime-core/src/lib.rs:2205)) возвращает `ToolOutcome { ok: false, kind: Denied }`.

**Исправляемый баг:** строка `"approval denied"` не содержит ни одного маркера из `tool_output_failed`, поэтому отклонённый пользователем вызов сейчас считается успешным и выставляет `mutation_done`. Delivery-gate пропускает задачу без фактической мутации.

**Критерии готовности:**

- регрессионный тест: отклонённый approval для `filesystem.write` не выставляет `mutation_done`;
- тест: `filesystem.read` файла со строкой `error:` внутри даёт `ok = true`;
- тест: `shell.execute` с выводом `test result: ok. 0 failed` даёт `ok = true`;
- тест: `shell.execute` с `exit_code = 1` и пустым stderr даёт `ok = false, kind = NonZeroExit`;
- тест: `git.commit` со `status: "nothing_to_commit"` не выставляет `commit_done`.

---

### Фаза 2. Адресное восстановление и эскалация

**Задача 2.1. Таблица подсказок по `ToolFailureKind`.**

Заменить единственную подсказку на [lib.rs:2270](../../crates/evohime-core/src/lib.rs:2270) функцией `recovery_hint(tool_name, kind, structured) -> Option<String>`:

| Kind | Подсказка |
| --- | --- |
| `NotFound` | назвать `filesystem.search` с фрагментом имени; напомнить, что пути workspace-relative |
| `InvalidInput` | вернуть JSON-схему **именно этого** инструмента из реестра, а не общий список примеров |
| `Denied` | объяснить, что повтор не поможет; предложить альтернативу без данного permission или отчёт хозяину |
| `Timeout` | сузить объём (конкретный путь вместо `.`, `--lib` вместо полного прогона) |
| `NonZeroExit` | процитировать первые N строк stderr из `structured`, а не весь вывод |
| `Execution` | общий fallback (текущий текст) |

Схема инструмента берётся из `tool_parameters(name)`, чтобы подсказка не расходилась с реальным контрактом.

**Задача 2.2. Счётчик подряд идущих провалов и эскалация.**

Добавить `consecutive_failures: HashMap<String, u32>`. Правила:

- успешный вызов инструмента обнуляет его счётчик;
- 2 провала подряд — подсказка усиливается («предыдущий подход не сработал дважды, смени способ»);
- 3 провала подряд — вызовы этого инструмента отклоняются **на стороне Core** на следующие K итераций: Ева получает отказ с указанием замены, но список `specs` не меняется;
- 5 провалов подряд по любым инструментам без единого успеха — остановка с `TaskFailed` и перечислением классов ошибок.

Инструмент **не** удаляется из `specs` в середине диалога: список тулов уходит провайдеру при каждом запросе, а в истории сообщений остаются `tool_call` уже исполненных вызовов; рассинхронизация списка и истории — известный источник ошибок валидации у OpenAI-совместимых провайдеров. Отказ на стороне Core даёт тот же обучающий эффект без риска.

Из эскалации исключены `filesystem.search`, `filesystem.list` и `filesystem.read` — лишившись всех средств разведки, Ева не сможет найти выход.

**Задача 2.3. Приоритет эскалации над delivery-gate.**

Delivery-gate ([lib.rs:2095](../../crates/evohime-core/src/lib.rs:2095)) при незакрытых требованиях подставляет «Задача ещё не завершена» и продолжает цикл. Это прямо конфликтует с решением об остановке из 2.2: gate будет гнать вперёд агента, который уже признан застрявшим.

Правило: **эскалация имеет приоритет.** При достижении порога остановки задача завершается `TaskFailed` с текстом, включающим и класс залипания, и список невыполненных требований доставки. Gate не имеет права продлить прогон после срабатывания эскалации.

**Задача 2.4. Починить дедупликацию вызовов — оба пути.**

Дедупликация существует в двух независимых местах, и оба хешируют сырую строку аргументов, поэтому лишний пробел обходит защиту:

- `seen_tool_calls` для обычных tool_call ([lib.rs:2074](../../crates/evohime-core/src/lib.rs:2074));
- `legacy_seen` для распарсенных legacy-вызовов ([lib.rs:1956](../../crates/evohime-core/src/lib.rs:1956), [lib.rs:2021](../../crates/evohime-core/src/lib.rs:2021)).

Исправления для обоих:

- ключ = `name` + канонизированный JSON (`serde_json::Value` с отсортированными ключами), а не сырая строка;
- вместо вечного бана — счётчик повторов: первый повтор блокируется с подсказкой, но после успешной мутации workspace счётчики чтения (`filesystem.read`, `filesystem.list`, `filesystem.search`) сбрасываются, потому что содержимое файлов изменилось и повторное чтение легитимно.

Дедуп-состояние выносится в единую структуру, используемую обоими путями, чтобы третий парсер не завёл третий обход защиты.

**Критерии готовности:**

- тест: `{"path":"a"}` и `{"path": "a"}` распознаются как один вызов — отдельно для обычного и для legacy-пути;
- тест: `filesystem.read` того же пути после успешного `filesystem.patch` не блокируется;
- тест: три подряд `InvalidInput` от одного инструмента приводят к отказу Core, а `specs` остаётся неизменным;
- тест: после срабатывания эскалации delivery-gate не продлевает прогон;
- тест: пять провалов без успеха завершают задачу событием `TaskFailed`.

---

### Фаза 3. Память в агентном цикле

Хранилище (`MemoryStoreSql::insert/search/list/archive`, [memory_store.rs](../../crates/evohime-local-storage/src/memory_store.rs)) и Core-обёртки ([lib.rs:1208](../../crates/evohime-core/src/lib.rs:1208)) уже есть. Агентный цикл к ним не обращается — это и есть отсутствующая петля обучения.

**Задача 3.0. Определить ключ scope (предпосылка, без неё фаза не реализуема).**

`MemoryStoreSql::SEARCH` требует `scope_kind` + `scope_id` ([memory_store.rs:190](../../crates/evohime-local-storage/src/memory_store.rs:190)), а `ToolContext` знает только `workspace_root`, `task_id` и опциональный `session_id` — никакого `project_id` в агентном цикле нет.

Решение: `scope_kind = Project`, `scope_id` = детерминированный хеш канонизированного `workspace_root`. Один репозиторий = один накопитель опыта; уроки не протекают между несвязанными workspace. Функция вывода scope живёт в `task_memory.rs` и покрыта тестом на стабильность при разном регистре и разделителях пути в Windows.

Если ответ на открытый вопрос 1 окажется другим — меняется только эта функция.

**Задача 3.1. Миграция схемы для счётчика подтверждений.**

Таблица `memory_entries` ([lib.rs:1693](../../crates/evohime-local-storage/src/lib.rs:1693)) не имеет поля для счётчика подтверждений, а `MemoryRecord` — соответствующего атрибута. Нужна миграция `PRAGMA user_version = 11` (текущий `SCHEMA_VERSION = 10`) с добавлением `confirmations INTEGER NOT NULL DEFAULT 1` и `lesson_key TEXT` + индексом по `(scope_kind, scope_id, lesson_key)`.

Миграция выполняется транзакционно с backup, как требует правило схемы. Кодировать счётчик внутри `content` нельзя: это ломает дедупликацию и делает вытеснение неработоспособным.

**Задача 3.2. Pre-task retrieval.**

Перед первой итерацией: lexical-поиск по промпту в scope из 3.0, top-K (K = 5, bounded по символам) записей класса «урок». Результат вставляется отдельным system-сообщением с явной пометкой, что это прошлый опыт, а не факт о текущем состоянии кода, и что он подлежит проверке.

Пустой результат, недоступная БД или ошибка поиска не блокируют задачу — retrieval деградирует молча, с записью в trace.

**Задача 3.3. Post-task запись урока.**

После завершения задачи (успех или провал) записать один bounded-урок:

- какой инструмент дал какой класс ошибки и что помогло;
- какая верификационная команда фактически прошла в этом workspace;
- какие пути оказались неверными и какие верными.

Формат урока — детерминированный, собирается Core из накопленной метрики прогона (фаза 5), **не** свободным текстом от модели: иначе память превращается в канал самовнушения. `provenance` = `task:<task_id>`, `privacy` — самый строгий из доступных, `expires_at` = +30 дней (в пределах `MAX_TTL_MS` = 31 день из `memory_domain`).

**Задача 3.4. Дедупликация и вытеснение.**

`lesson_key` = хеш от (инструмент + класс ошибки + нормализованная цель). При совпадении ключа в scope — инкремент `confirmations` и обновление `created_at`, без новой записи. Лимит числа уроков в scope с вытеснением наименее подтверждённых и наиболее старых.

**Критерии готовности:**

- тест: два прогона с одинаковым провалом дают одну запись с `confirmations = 2`;
- тест: `scope_id` стабилен для `C:\github\EvoHime` и `c:/github/evohime`;
- тест: retrieval не роняет задачу при пустой или недоступной БД;
- тест: в записанном уроке нет содержимого файлов, токенов и абсолютных путей за пределами workspace;
- тест: миграция 10 → 11 идемпотентна и сохраняет существующие записи;
- ручная проверка: вторая задача того же типа проходит за меньшее число итераций, чем первая (замеряется метриками из фазы 5).

---

### Фаза 4. Устойчивость процессов и провайдера

**Задача 4.1. Сброс бюджета рестартов supervisor.**

[windows_supervisor.rs:237](../../crates/evohime-supervisor/src/windows_supervisor.rs:237) — `restarts` растёт монотонно за весь срок жизни supervisor. Core, проработавший неделю и упавший в четвёртый раз, больше не поднимется.

- сбрасывать счётчик, если предыдущая генерация прожила дольше порога здорового аптайма (по умолчанию 10 минут, env-override);
- заменить линейный `sleep(250 * restarts)` (максимум 750 мс) на экспоненциальный backoff с jitter и потолком;
- залогировать причину сброса в `supervisor.jsonl`.

**Задача 4.2. Ретрай оборванного стрима.**

[retry.rs](../../crates/model-gateway/src/retry.rs) покрывает только запрос до первого SSE-токена. Обрыв в середине генерации теряет всю итерацию.

- при обрыве до появления tool_call — повтор итерации целиком (побочных эффектов ещё нет, повтор идемпотентен);
- при обрыве после начала tool_call — не повторять, вернуть в цикл как `Execution`-ошибку с подсказкой из фазы 2;
- ограничить число таких повторов на задачу.

**Задача 4.3. Различать классы 429.**

`is_retryable_status` считает все 429 одинаковыми. При троттлинге 190 запросов/час (коммит `467ca1a`) «подожди секунду» и «квота исчерпана» требуют разных решений.

Правило: явный признак исчерпания (заголовок/тело провайдера) → без ретраев, переключение маршрута через `routing_runtime` или честная остановка с сообщением хозяину. При неоднозначности — ограниченный backoff (не более 2 попыток, с учётом `Retry-After`), и только после их исчерпания — трактовка как исчерпание квоты.

Дефолт «сразу считать исчерпанием» отвергнут: он превращает обычный transient rate-limit в отказ задачи, то есть регрессию относительно текущего поведения.

**Критерии готовности:**

- тест: генерация, прожившая дольше порога, обнуляет счётчик рестартов;
- тест: backoff ограничен потолком и не вырождается в ноль;
- тест: обрыв стрима до tool_call приводит к ровно одному повтору;
- тест: 429 с явным признаком исчерпания не ретраится; неоднозначный 429 ретраится не более двух раз.

---

### Фаза 5. Наблюдаемость и измеримость

Без этой фазы улучшения из фаз 1–4 недоказуемы, а фаза 3 не имеет источника данных для уроков.

**Задача 5.1. Подключить `observability.rs`.**

Модуль ([observability.rs](../../crates/evohime-core/src/observability.rs)) содержит готовый bounded-контракт хуков `BeforeContext / BeforeTool / AfterTool / BeforeCommit / AfterTask` и помечен «намеренно не подключён к `lib.rs`». Подключить к агентному циклу, сохранив bounded/redacted гарантии контракта.

**Задача 5.2. Метрики прогона.**

Собирать за задачу и писать в БД: число итераций, вызовов по инструментам, успехов/провалов по `ToolFailureKind`, число сработавших recovery-подсказок, факт эскалации, итоговый статус. Эти же данные — вход для задачи 3.3.

**Задача 5.3. Заменить подстроковые эвристики delivery-gate.**

[lib.rs:2243](../../crates/evohime-core/src/lib.rs:2243) засчитывает верификацию по `arguments.contains("test") || "check" || "build" || "собер"` — `echo "check"` проходит как успешная проверка. Разбирать фактическую команду (первый исполняемый токен + подкоманда) и требовать `exit_code = 0` из `structured`. Порог `research_observations >= 5` заменить на проверку фактического покрытия (был ли прочитан хотя бы один файл, релевантный цели), а не на магическое число.

Внедряется через shadow-режим из раздела 3: расхождение старой и новой оценки сначала логируется, строгий режим включается после сверки.

**Критерии готовности:**

- тест: `echo "check"` не выставляет `verification_test_passed`;
- тест: `cargo test` с ненулевым кодом не выставляет флаг;
- отчёт Core Doctor ([doctor.rs](../../crates/evohime-core/src/doctor.rs)) получает check со сводкой success rate инструментов за последние N задач.

## 5. Порядок реализации

| Шаг | Содержание | Обоснование приоритета |
| --- | --- | --- |
| 1 | Задача 1.3 в части approval-бага | активный дефект, ломает delivery-gate |
| 2 | Фаза 1 целиком | предпосылка для фаз 2, 3, 5 |
| 3 | Фаза 2 | наибольший эффект на живучесть прогона |
| 4 | Задача 4.1 | дёшево, ощутимо для аптайма |
| 5 | Фаза 5 | без метрик нельзя ни подтвердить фазу 3, ни собрать её уроки |
| 6 | Задача 3.0 + 3.1 | scope и миграция — предпосылки остальной фазы 3 |
| 7 | Задачи 3.2–3.4 | наибольший выигрыш, но зависит от 1, 2, 5 |
| 8 | Задачи 4.2, 4.3 | зависят от поведения провайдера, можно параллельно |

## 6. Риски

- **Память как канал самовнушения.** Урок со слов модели закрепит ошибочный вывод. Смягчение: уроки собирает Core из метрик, TTL конечный, счётчик подтверждений, retrieval помечен как непроверенный опыт.
- **Регрессия delivery-gate.** Ужесточение проверок (1.2, 5.3) начнёт блокировать задачи, которые раньше проходили — но проходили ложно. Смягчение: shadow-режим и сверка расхождений до включения.
- **Эскалация как новый способ застрять.** Смягчение: отказ на стороне Core вместо удаления из `specs`, временное окно K итераций, разведывательные инструменты не блокируются никогда.
- **Миграция схемы 10 → 11.** Любая правка схемы рискует данными. Смягчение: транзакционность и backup по действующему правилу, тест идемпотентности, отдельный шаг в порядке реализации.
- **Накопление флагов.** Kill-switch из раздела 3 может остаться навсегда и превратиться в два несогласованных поведения. Смягчение: снятие флага — обязательный пункт закрытия каждой фазы.

## 7. Проверки перед заявлением о готовности

```powershell
cargo test -p evohime-core -p evohime-local-storage -p evohime-tool-runtime -p evohime-model-gateway
cargo check -p evohime-supervisor
git diff --check
```

Плюс ручной прогон `.\start-dev.ps1` с задачей, требующей мутации и верификации, и сверка `core.jsonl` на наличие событий эскалации и записи урока.

## 8. Открытые вопросы к хозяину

1. Scope памяти — `Project` по хешу workspace (принято как рабочий дефолт в 3.0) или что-то шире, чтобы опыт переносился между репозиториями?
2. Показывать ли извлечённые уроки в UI перед стартом задачи (прозрачность) или держать их только в контексте модели?
3. Нужен ли approval на запись урока, или запись в локальную БД считается безопасной по умолчанию?

## 9. Изменения относительно v1

Результат самостоятельного ревью. Блокирующие правки помечены **[B]**.

| # | Находка | Правка |
| --- | --- | --- |
| 1 | **[B]** Не определён `scope_id` для поиска в памяти; в агентном цикле нет `project_id` | Новая задача 3.0 — вывод scope из `workspace_root` |
| 2 | **[B]** Счётчика подтверждений нет в схеме `memory_entries` | Новая задача 3.1 — миграция `user_version = 11` |
| 3 | **[B]** Удаление инструмента из `specs` рассинхронизирует список тулов с историей `tool_call` | 2.2 — отказ на стороне Core вместо правки `specs` |
| 4 | **[B]** Эскалация и delivery-gate конфликтуют: gate продлевает прогон уже застрявшего агента | Новая задача 2.3 — правило приоритета |
| 5 | Второй путь дедупликации (`legacy_seen`) не был учтён | 2.4 покрывает оба пути через общую структуру |
| 6 | Требование поля `ok` во всех `structured` избыточно: провал маскируют только `shell.execute` и `git.commit` | 1.2 сужена до двух инструментов |
| 7 | `ToolOutcome` внутри `ToolResult` дублирует то, что уже несёт `Result`/`ToolError` | 1.1 — классификация в Core, `ToolResult` не меняется |
| 8 | Сохранённый текстовый fallback вернул бы ровно те ложные срабатывания, ради которых затевается фаза | 1.1 — `tool_output_failed` удаляется целиком |
| 9 | Дефолт «неоднозначный 429 = исчерпание квоты» — регрессия для transient rate-limit | 4.3 — ограниченный backoff, затем исчерпание |
| 10 | Нет отката и поэтапного включения | Новый раздел 3 — kill-switch и shadow-режим |
