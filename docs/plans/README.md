# Планы реализации

Каталог `docs/plans/` хранит временные планы реализации. Каждый план проходит
ревью, выполняется по этапам и удаляется после полного завершения. Контракт
завершённого плана переносится в [`../architecture.md`](../architecture.md), а
подтверждённое состояние — в [`../current-state.md`](../current-state.md).

Планы 01–20 завершены и удалены после реализации. Новые планы добавляются
сюда только после ревью.

## Формат и порядок

Имя файла имеет формат `NN-M-slug.md`:

- `NN` — номер направления;
- `M` — этап внутри направления;
- `M = 0` — обзор направления.

Список файлов в алфавитном порядке задаёт порядок реализации. Блокирующая
зависимость допускается только от более раннего номера направления или от
более раннего этапа того же направления.

Каждый этап обязан разделять зависимости на:

1. **Блокирующие** — без них этап невыполним.
2. **Опциональные** — без них этап всё равно выполняется, но документ должен
   описывать предсказуемую деградацию.

Циклы зависимостей запрещены. Взаимная связь двух этапов допустима только
если одна сторона блокирующая, а другая — опциональная с описанной деградацией.

## Источники истины

- [`../current-state.md`](../current-state.md) — что подтверждённо реализовано
  в текущем checkout;
- [`../architecture.md`](../architecture.md) — действующие контракты и
  архитектурные границы;
- [`../development-plan.md`](../development-plan.md) — исполняемый план
  текущего desktop-цикла;
- [`../roadmap.md`](../roadmap.md) — долгосрочные направления;
- [`../release-audit.md`](../release-audit.md) — технические release-gates и
  оставшиеся решения, блокирующие выпуск.

Этот файл не является источником статуса реализации и не заменяет указанные
документы.

## Текущий план

Планы 01–20 реализованы и перенесены в канонические разделы
[`../architecture.md`](../architecture.md) и [`../current-state.md`](../current-state.md).
На ревью находятся обзорный план [`21-0-reliability-and-recovery.md`](21-0-reliability-and-recovery.md) и этапы 21.1–21.4. До утверждения это только планы, реализация не начата.
