[package]
name = "evohime-cli-protocol"
version = "0.1.0"
publish = false
edition = "2021"

[dependencies]
evohime-desktop-ipc = { path = "../desktop-ipc" }
prost = "0.14"
tokio = { version = "1", features = ["io-util", "macros", "rt", "time"] }
uuid = { version = "1", features = ["v4"] }
