[package]
name = "evohime-receipts"
version = "0.1.0"
edition = "2021"

[dependencies]
ring = "0.17"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
thiserror = "2"

[dev-dependencies]
hex = "0.4"

[build-dependencies]
serde_json = "1"
