[package]
name = "evohime-project-index"
version = "0.1.0"
edition = "2021"

[lib]
path = "src/lib.rs"

[dependencies]
walkdir = "2"
# Wave 4: Embeddings & semantic search
sha2 = "0.10"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "1"

[dev-dependencies]
tempfile = "3"
