# permissions

Permission policy types used by the tool runtime and approval flow.

The crate provides permission classification, ask/allow/deny decisions, and one-shot approvals for the Rust Core approval flow. IPC delivery and resume handling are owned by the current desktop runtime.
