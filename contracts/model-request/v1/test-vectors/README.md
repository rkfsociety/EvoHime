# Model request v1 vectors

Векторы добавляются вместе с контрактными изменениями. Канонические bytes
вычисляются RFC 8785 JCS с domain-separated SHA-256:
`evohime-model-request-v1\\0 || canonical_bytes`.
