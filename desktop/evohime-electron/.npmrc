# Lifecycle scripts are disabled by default: only the installers listed in
# scripts/postinstall-allowlist.mjs may run, and they run explicitly through
# `npm run bootstrap` (plan 0, stage 2 — dependency supply chain).
ignore-scripts=true
audit=false
fund=false
save-exact=true
