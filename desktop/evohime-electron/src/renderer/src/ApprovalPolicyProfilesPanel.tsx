import React,{useState}from'react';import{useShellApi}from'./shell-api';import type{ConnectionState}from'@shared/api';
export function ApprovalPolicyProfilesPanel({connection}:{readonly connection:ConnectionState}):React.JSX.Element{const api=useShellApi();const[result,setResult]=useState<unknown>(null);const[message,setMessage]=useState('');const run=async()=>{if(!api||connection!=='connected'){setMessage('Нет подключения к Core.');return}const r=await api.invoke('core.approvalPolicyProfiles',{operation:'list'});if(r.ok){setResult(r.value);setMessage('Политики загружены.')}else setMessage(r.message)};return <section aria-label="Approval Policy Profiles"><h3>Approval Policy Profiles</h3><p>Policy решает prompt requirement, но не снимает hard requirements и ExecutionPolicy.</p><button type="button" onClick={()=>void run()}>Обновить политики</button>{result?<pre>{JSON.stringify(result,null,2)}</pre>:null}{message?<p role="status">{message}</p>:null}</section>}
