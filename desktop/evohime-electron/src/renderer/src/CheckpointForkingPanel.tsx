import React,{useState}from'react';import{useShellApi}from'./shell-api';import type{ConnectionState}from'@shared/api';
export function CheckpointForkingPanel({connection}:{readonly connection:ConnectionState}):React.JSX.Element{const api=useShellApi();const[id,setId]=useState('fork-run');const[payload,setPayload]=useState('{"schema_version":1,"source_checkpoint_id":"checkpoint","parent_run_id":"run","workspace_fingerprint":"fingerprint","overrides":[]}');const[msg,setMsg]=useState('');const run=async()=>{if(!api||connection!=='connected'){setMsg('Нет подключения к Core.');return}const r=await api.invoke('core.checkpointForking',{operation:'fork',forkRunId:id,payload});setMsg(r.ok?'Новая ветка создана без replay effects.':r.message)};return <section aria-label="Checkpoint Forking"><h3>Checkpoint Fork & Replay</h3><p>Ветка immutable относительно источника; внешние эффекты автоматически не повторяются.</p><input value={id} onChange={e=>setId(e.target.value)}/><textarea value={payload} onChange={e=>setPayload(e.target.value)} maxLength={256*1024}/><button type="button" onClick={()=>void run()}>Создать ветку</button>{msg?<p role="status">{msg}</p>:null}</section>}
