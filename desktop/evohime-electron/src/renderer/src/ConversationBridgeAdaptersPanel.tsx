import React,{useState}from'react';import{useShellApi}from'./shell-api';import type{ConnectionState}from'@shared/api';
export function ConversationBridgeAdaptersPanel({connection}:{readonly connection:ConnectionState}):React.JSX.Element{const api=useShellApi();const[msg,setMsg]=useState('');const run=async(operation:'list'|'clear')=>{if(!api||connection!=='connected'){setMsg('Нет подключения к Core.');return}const r=await api.invoke('core.conversationBridgeAdapters',{operation,bridgeId:'ui'});setMsg(r.ok?'Команда принята Core.':r.message)};return <section aria-label="Conversation Bridge Adapters"><h3>Conversation Bridge</h3><p>Внешние threads не получают credentials, workspace paths, raw prompts или capabilities. Pairing и revoke выполняются только через Core.</p><button type="button" onClick={()=>void run('list')}>Показать состояние</button><button type="button" onClick={()=>void run('clear')}>Отозвать bridge</button>{msg?<p role="status">{msg}</p>:null}</section>}
