import React,{useState}from'react';import{useShellApi}from'./shell-api';import type{ConnectionState}from'@shared/api';
export function StandingApprovalProfilesPanel({connection}:{readonly connection:ConnectionState}):React.JSX.Element{const api=useShellApi();const[result,setResult]=useState<unknown>(null);const[message,setMessage]=useState('');const run=async()=>{if(!api||connection!=='connected'){setMessage('Нет подключения к Core.');return}const r=await api.invoke('core.standingApprovalProfiles',{operation:'list'});if(r.ok){setResult(r.value);setMessage('Профили загружены.')}else setMessage(r.message)};return <section aria-label="Standing Approval Profiles"><h3>Standing Approval Profiles</h3><p>Узкие пользовательские grants; ExecutionPolicy и hard-deny всегда проверяются Core.</p><button type="button" onClick={()=>void run()}>Обновить профили</button>{result?<pre>{JSON.stringify(result,null,2)}</pre>:null}{message?<p role="status">{message}</p>:null}</section>}
