<Window
    x:Class="EvoHime.Desktop.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <Grid Padding="32" RowSpacing="16">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>
    <TextBlock Text="EvoHime" FontSize="28" />
    <TextBlock x:Name="ConnectionStatus" Grid.Row="1" Text="Не подключено" />
    <Grid Grid.Row="2" RowSpacing="12">
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto" />
        <RowDefinition Height="*" />
      </Grid.RowDefinitions>
      <TextBox x:Name="PromptBox" Header="Задача" PlaceholderText="Что нужно сделать?" AcceptsReturn="True" TextWrapping="Wrap" />
      <ScrollViewer Grid.Row="1" MaxHeight="360">
        <TextBlock x:Name="EventLog" TextWrapping="Wrap" />
      </ScrollViewer>
    </Grid>
    <StackPanel Grid.Row="3" Orientation="Horizontal" Spacing="12">
      <Button x:Name="StartButton" Content="Запустить" Click="StartButton_Click" />
      <Button x:Name="StopButton" Content="Stop" Click="StopButton_Click" IsEnabled="False" />
    </StackPanel>
  </Grid>
</Window>
