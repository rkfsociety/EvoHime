{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "cd \"$CLAUDE_PROJECT_DIR\" 2>/dev/null && git status --short 2>/dev/null | grep -v '^??' | grep -q . && printf '%s' '{\"decision\":\"block\",\"reason\":\"Tracked files in the EvoHime repo have uncommitted changes. Per AGENTS.md rule 6 (task-only commit after changes), commit them now, or explicitly tell the user why they are being left uncommitted, before ending this turn.\"}' || true"
          }
        ]
      }
    ]
  }
}
